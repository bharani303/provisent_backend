package com.example.provisent_backend.Repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.example.provisent_backend.Model.Pop_menu;

public interface PopMenuRepository extends JpaRepository<Pop_menu, Long> {
}